{
  "name": "goods-showcase",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}

